# vim: ft=gitconfig:
[user]
  name = Robert Iannucci
[core]
  autocrlf = false
  filemode = false
  excludesfile = ~/.config/nvim/rando_dotfiles/gitexcludes
  pager = ~/.config/nvim/rando_dotfiles/bin/less_helper
[color]
  ui = true
[alias]
  udiff  = upstream-diff
  reup   = rebase-update
  squash = squash-branch
  root   = checkout origin/master
  nb     = new-branch
  tb     = new-branch --upstream_current
  rp     = reparent-branch
  rpr    = reparent-branch --root
  rename = rename-branch
  up     = nav-upstream
  down   = nav-downstream
  smap   = map-branches -vv
[push]
  default = upstream
[depot-tools "upstream-diff"]
	default-args = --wordwise
