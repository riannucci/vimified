# vim: ft=gitconfig:
[user]
	name = Robert Iannucci
[core]
	autocrlf = false
	filemode = false
[color]
  ui = true
[alias]
  udiff = !git diff $(git merge-base HEAD $(git config branch.$(git rev-parse --abbrev-ref HEAD).merge))
  map = !git log --graph --full-history --all --color --date=short --pretty=format:'%x1b[31m%h%x09%x1b[32m%d%x1b[0m%x20%ad %s' $(git config iannucci.mapextra 2> /dev/null) || true
  bclean = !git branch --merged master | awk '!/master/{print $NF}' | xargs git branch -d
  reup = !~/.vim/gitscripts/rebase_all.py
  squash =  !git reset --soft $(git merge-base master HEAD) && git commit -C HEAD@{1} -a
  freeze =  commit -am 'FREEZE'
  thaw   =  !((git log --pretty=%s -n1 | grep FREEZE -q) && git reset 'HEAD~') || echo 'Nothing to thaw.'
[credential]
	helper = osxkeychain
